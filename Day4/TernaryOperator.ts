//Ternary Operator
let age: number = 20;
let isAdult: string;

// Using ternary operator to check if the person is an adult
isAdult = (age >= 18) ? "Yes, Adult" : "No, Not Adult";

console.log("****Ternary Operator****");
console.log(`Is person adult? : ${isAdult}`); // Output: Yes, Adult